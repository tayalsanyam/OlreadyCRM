/** Client-safe uploader verification constants (no DB imports). */

/** Minimum connect attempts before NI / archive on pending verification. */
export const MAX_VERIFICATION_CONNECT_ATTEMPTS = 2;
