/** Client-safe roster status labels (no server/db imports). */
export const MUA_STATUS_LABELS: Record<string, string> = {
  active: "Active — on roster",
  inactive: "Inactive — hidden from assignment",
};
