/** Default max active MUA conversations per bride lead (from sla_config). */
export const DEFAULT_PER_LEAD_CAP = 12;

/** Non-plan MUAs in commission have no plan tier cap — use a practical weekly default. */
export const COMMISSION_NON_PLAN_WEEKLY_CAP = 99;
