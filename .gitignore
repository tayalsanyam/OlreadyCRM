# Olready CRM — git publish bundle (copy to deploy/repo/.gitignore)

node_modules/
.next/
out/
dist/

.env
.env.local
.env.*.local

*.log
.DS_Store
tsconfig.tsbuildinfo
.eslintcache

db/backups/
uploads/*
!uploads/.gitkeep
!uploads/**/.gitkeep
